<% extend 'layouts/preregistration' %>
<!-- Page Content -->
<div class="container">

    <div class="row">

        <div class="col-lg-12  col-md-12">
            <form class="form-horizontal" role="form" action="/preregistration/upload", method="POST", enctype="multipart/form-data">

                <!-- 画像 -->
                <div class="form-group">
                    <label class="col-lg-2 col-md-2 control-label">Image</label>
                    <div class="col-lg-2 col-md-2">
                        <input id="lefile" name="image" class="form-control" type="file" style="display:none">
                    </div>
                    <div class="input-append">
                        <input id="photoCover" class="input-large" type="text">
                        <a class="btn" onclick="$('input[id=lefile]').click();">Browse</a>
                    </div>

                    <script type="text/javascript">
                        $('input[id=lefile]').change(function() {
                            $('#photoCover').val($(this).val());
                        });
                    </script>
                    <div class="">
                    <label class="col-lg-2 col-md-2 control-label"></label>
                    <div class="col-lg-2 col-md-2">
                        <input type="submit" class="form-control">
                    </div>
                </div>
                </div>
            </form>
            <div class="row">
                <% for preregistrationGoods in @preregistrationGoodsList : %>
                    <!-- user goods -->
                    <% include 'elements/preregistration_goods', {preregistrationGoods: preregistrationGoods} %>
                <% end %>
            </div>
        </div>
    </div>
</div>
<!-- /.container -->