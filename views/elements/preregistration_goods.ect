<div class="col-md-3 col-sm-12 col-xs-12">
    <div class="thumbnail">
        <img src="<%= @preregistrationGoods.imagePath %>" alt="">
    </div>
    <div id="comment-<%= @preregistrationGoods.id %>">
    <% for comment in @preregistrationGoods.commentList : %>
        <p><%= comment %></p>
    <% end %>
    </div>
    <div class="">
        <form class="form-inline">
            <!-- comment -->
            <div class="row">
                <div class="col-md-10 col-sm-10 col-xs-10">
                    <input type="text" class="form-control" placeholder="コメントする..." id="<%= @preregistrationGoods.id %>">
                </div>
                <button type="button" class="col-md-2 col-sm-2 col-xs-2 comment-request btn btn-primary" data-preregistration-goods-id="<%= @preregistrationGoods.id %>">
                    送信
                </button>
            </div>
        </form>
    </div>
</div>
<div class="col-md-12 col-sm-12 col-xs-12">
    　
</div>